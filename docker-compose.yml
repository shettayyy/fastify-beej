version: '3.9'
services:
  fastifybeej:
    image: rashtay/fastifybeej
    env_file:
      - .env
      - .env.production
    restart: unless-stopped
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - ${PORT}:${PORT}
